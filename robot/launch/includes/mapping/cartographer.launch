<launch>
  <arg name="odom_topic" default="/mobile_base/odom">

  <node name="mapping" pkg="cartographer_ros" type="cartographer_node" args="-configuration_directory $(find robot)/config -configuration_basename cartographer.lua">
    <remap from="odom" to="$(arg odom_topic)"/>
  </node>

  <node name="occupancy_grid" pkg="cartographer_ros" type="cartographer_occupancy_grid_node" args="-resolution 0.025"/>
</launch>
